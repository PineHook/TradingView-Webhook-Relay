# PineHook Release Hashes.
# Format: <64-hex-sha256>  <filename>
# Lists the currently-served bundle only. Superseded releases are pruned
# when a new bundle is published, so old versions are never distributed.
# Verify your download at any time with:
#   sha256sum --check SHA256SUMS.txt
#
# For the user-facing guide see:
#   https://pinehook.io/docs/security/verifying-downloads
